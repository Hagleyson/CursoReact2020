import React from "react";
function Header() {
  const styles = {
    width: "100%",
    height: "100px",
    backgroundColor: "green"
  };
  return <header style={styles} />;
}

export default Header;
